<?xml version="1.0"?>
<odoo>
    <record id="operator_action_pos_config_kanban" model="ir.actions.act_window">
        <field name="name">Dashboard</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">pos.config</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="domain"></field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new PoS
            </p><p>
                Configure at least one Point of Sale.
            </p>
        </field>
    </record>
 
    <!-- MENUS POUR L'OPERATEUR DE COMPTOIR DE CHANGE-->
    <menuitem name="Comptoir de Change (operateurices)"
        id="cdc_operator_menu_root"
        parent="base.menu_custom">
        <!-- MENUS POS -->
        <menuitem id="exchange_counter_operator_pos" name="Comptoir de Change">
            <menuitem id="operator_menu_pos_config" action="operator_action_pos_config_kanban"/>
        </menuitem>
    </menuitem>
</odoo>